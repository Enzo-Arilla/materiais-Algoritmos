#include <stdio.h>

int main() {
    int evento;
    char corrida;
    float tempo = 27.25;
    evento = 5;
    corrida = 'C';
    printf("O tempo vitorioso na eliminatoria %c", corrida);
    printf("\nda competicao %d foi %f\n", evento, tempo);
    return 0;
}